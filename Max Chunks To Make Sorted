public class Solution {
    public int solve(int[] A) {
    int ms=0;
    int ch=0;
    int n=A.length;
    for(int i=0;i<n;i++)
    {
        ms=Math.max(ms,A[i]);
        if(ms==i)
        {
            ch++;
        }

    }
    return ch;

    }
}
